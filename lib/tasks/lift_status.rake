#desc "Fetch lift status"
#task: fetch_status => :environment do
  #require 'rubygems'
  #require 'mechanize'
  #agent = Mechanize.new

  # page = agent.get("http://www.whistlerblackcomb.com/mountain-info/snow-report#lifts-and-trails")
  # lift_status = agent.page.css("g#Lift_Bubbles")

#end

# maybe the best step forward is to figure out the snow report, then come back to lift lines